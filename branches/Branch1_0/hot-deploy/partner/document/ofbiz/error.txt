1.ServerHitBin.saveHit()方法中关于MySql允许错误的解释（https://issues.apache.org/jira/browse/OFBIZ-2208）
	20120118：重新修改该问题，新增id字段，修改类ServerHitBin.saveHit()方法，增加serverHit.set("id", UUID.randomUUID().toString());。
				修改文件framework\webapp\entitydef\entitymodel.xml，增加id字段，并设置为主键。

2.ServerHitType表找不到外键问题，增加hot-deploy\partner\data\ContentTypeData.xml文件，初始化hitType类型。

3.LoginServices.createUserLogin()方法partyId缺失的问题，增加传入值的判断。
		if (UtilValidate.isNotEmpty(partyId)) {
        	userLoginToCreate.set("partyId", partyId);
        }
        
4.在文件fieldtypemysql.xml中增加相应的类型。